# phase E 独立验证结果

日期：2026-09-11。结论：`0x59490503` phase E 实车采集通过预先冻结的数据质量门；未经重拟合的 C 模型
通过 E 独立运动预测门和指令—反馈电流一致性门。当前状态为
`closed_loop_candidate_passed_C_E_gates`。该模型描述现有 PID 闭环参考映射，不是开环 MPC 被控对象，
`hardware_takeover_allowed=false`。

## 数据身份与现场终态

- 目标机：`/home/nuc11--02/yaw-e-trace-59490503/captures/e_validation_20260911_194400/dual_e`
- 本地副本：`NoMachineTemp/e-validation-20260911-194400`
- 固件/trace/phase：`0x59490503` / v5 / E（profile 4、reverse 2）
- trial 1 正常结束：phase 5、reason 0、`big_stop_reason=none`
- 原始流 SHA256：`6278D10CD534FE0356C913258307EB5B124351C574BBC9D743B7591767393031`
- 原现场报告 SHA256：`9996802F9A3541844E256BEE655E9EA40A603C7ABC6A77C950430DEF833B5D72`
- 修正尾部解释后的回放报告 SHA256：
  `96FCB0335352FEE2EE5D310F9506F20EFA07B47A2B606C66B38D893846E3F825`
- 数据审计报告 SHA256：`C15F25B7060E1ADE7C79FF9C188FD1CCCC7E7B8318878A5437A132460C9D7C2F`
- 冻结模型验证报告 SHA256：`A3AB65DF64D7ECD053F73EF446A765AF744B30C589A34BC6275FC644D553E218`

验证完成后，主机重新 probe 到 UP、终态成功、输出关闭，再发送 RELEASE。最终 probe 为 phase 0、reason 0、
`flags=252`，trial 所有权已释放。

## 串口尾部判定修复

现场首份报告显示 `download_complete=false` 和 `incomplete_stream`，但记录、终止 metadata 和所有数据帧均
已完整解析。原始流最后一个字节是下一个 KEEPALIVE/RECEIPT 状态回复的帧首 `0xFF`。旧主机规则只把
`FF 37` 两字节及更长前缀识别为“完整 trial 后的部分状态帧”，没有覆盖串口读取恰好停在单个 `FF` 的情况。

解码器现仅在 trial 自身已经满足 terminal metadata、记录数相等且没有残留数据分组时，把单个 `FF` 识别为
后续状态帧前缀。数据流内部的单字节、未完成 trace 前缀和 CRC 错误仍然拒绝。新增单/双字节尾部测试后，
30 项 CAN/双参考/E 协议测试通过，2 项因测试目录未复制历史夹具跳过。对原始流重新回放得到：

- `download_complete=true`、`quality_issues=[]`、CRC 0；
- 8990 条记录，终端 metadata count 同为 8990；
- 尾部 1 字节，`buffered_wire_is_partial_status_after_complete=true`；
- 流内丢弃字节 0，CAN failed/errors/aborted 均为 0。

该修复只改变主机对 trial 完成后的串口分块边界解释，不改动固件、波形、记录或验收阈值。
采集后只读包为 `NoMachineTemp/yaw-e-trace-59490503-hostfix1.zip`，SHA256：
`AEC6BFD7C19D6648655F33754F775F0E0009E43AB8164B6D0A32233EE6356BB2`。包内只开放 replay、probe 和
release，所有运动动作已禁用；目标机 `/tmp` 的16项哈希和真实 E 原始流回放均通过。

## 预声明采集质量门

| 项目 | E 结果 | 冻结门限 |
|---|---:|---:|
| 记录数 | 8990/9001，短缺 11（0.122%） | 最多短缺 45（0.5%） |
| 时间跨度 | 35.999737 s | 完整覆盖 36 s |
| 首/末时间戳 | 0.430 ms / 36.000167 s | 末端覆盖 36 s |
| 最小/最大间隔 | 1.870 / 6.858 ms | 正数且不超过 10 ms |
| 参考回读最大误差 | 0.009305° | 不超过 0.011° |
| 双参考秩/相关系数 | 2 / 0.013402 | 秩 2，相关绝对值不超过 0.1 |

`full_duration_timing_accepted=true`，短缺记录按微秒时间戳和每区间指令积分重采样。固件采集质量锁存为
false。大/小 yaw 最大反馈年龄为 1.171/2.511 ms，且每个有效区间的 CAN 覆盖完整。

## 冻结 C 模型在 E 上的结果

模型系数、中心、尺度、12/4/0 阶次、ridge=1 和30%门限均来自 E 采集前固定的 C 报告；E 没有重新拟合
运动模型。

| 输出 | 冻结模型 RMSE | 保持预测 RMSE | 改善率 | 判定 |
|---|---:|---:|---:|---|
| 大 yaw 关节 | 0.2012° | 0.3760° | 46.49% | 通过 |
| 小 yaw 关节 | 0.5322° | 0.8077° | 34.11% | 通过 |
| 航向角（诊断项） | 0.1433° | 0.3483° | 58.86% | 非两关节主门限 |

两个关节均超过预声明的30%最低改善率。E 实测相对运动峰值为大 yaw 2.0215°、小 yaw 1.8896°、航向
1.3704°，没有触发行程或实测速度保护。

## 指令—反馈电流一致性

| 轴 | C 增益 | E 增益 | 相对差 | C/E 延迟 | 判定 |
|---|---:|---:|---:|---:|---|
| 大 yaw | 0.7480 | 0.7216 | 3.53% | 0/1 个采样 | 通过 |
| 小 yaw | 0.7451 | 0.7537 | 1.16% | 2/2 个采样 | 通过 |

增益差低于10%，延迟差不超过1个采样。这里的电流仍是反馈原始量，截距合并了传感器零偏、重力、线缆
载荷和静摩擦，不能解释为已标定力矩。

## 残差结构和下一阶段

| 输出 | 100 ms 残差自相关 | 0.5 Hz 以下功率占比 |
|---|---:|---:|
| 大 yaw 关节 | 0.964 | 87.15% |
| 小 yaw 关节 | 0.979 | 92.73% |
| 航向角 | 0.952 | 79.99% |

冻结模型通过相对保持预测门，但残差仍明显有色，尤其小 yaw 存在约 -0.325°的平均误差。这与方向相关
摩擦、线缆回正力、姿态负载、慢偏置和闭环积分状态尚未进入模型相符。

下一阶段不再采 C/D/E。先用现有 C/D/E 做方向、速度、姿态和电流偏置条件下的残差分层，比较加入常值
偏置、方向摩擦/滞环和慢状态后的严格留出效果。若目标仍是 MPC 硬件接管，还需单独设计执行器到关节的
开环或已知内环植物辨识，并完成影子预测、制动裕量和接管状态机验证。

上述离线工作现已完成，结果见
[`CDE_RESIDUAL_FRICTION_REVIEW_20260911.zh-CN.md`](CDE_RESIDUAL_FRICTION_REVIEW_20260911.zh-CN.md)：
简单偏置和仅参考量修正不能泛化；依赖上一采样实测状态的观测器修正候选在逐相位留出中显著改善，
但 C/D/E 均已参与结构选择，仍需新的预声明独立数据才能验证该修正器。
