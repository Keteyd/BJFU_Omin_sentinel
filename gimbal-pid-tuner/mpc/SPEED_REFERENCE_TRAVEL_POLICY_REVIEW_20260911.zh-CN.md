# 去线缆速度参考行程策略复核

日期：2026-09-11；固件：`0x59490702`；适用范围：仅 S1/S2 速度参考 profile。

S1 在 `0x59490701` 上运行到约 20.284 s 后以 `reason=travel` 中止。终止样本的 IMU 航向角相对起点为
`20.0578°`，刚超过旧 `20°` 阈值；大 yaw 相对运动约 `21.533°`，小 yaw 相对运动约 `12.920°`。
CAN 发送无失败、反馈保持新鲜、两轴实测速率和软件输出均未触及各自上限。因此这次中止由去线缆工况下
仍保留的航向相对行程阈值导致。

按操作员审核，`0x59490702` 对速度参考 profile 关闭大 yaw 和航向相对起点行程中止。小 yaw 的编码器
绝对机械限位和相对起点 `20°` 限制继续生效；实测速率、反馈、遥控停止、心跳、有限数、调度周期以及
速度 PID 输出限幅也继续生效。其他采集 profile 不受影响。

验证证据：六个本地 C 测试目标全部通过，其中新增用例确认大 yaw/航向超过旧阈值仍可继续，而小 yaw
超过 `20°` 会以 travel 中止；Python 协议、波形、调度相位与端到端流测试共 34 项通过；Keil Arm
Compiler 6.22 构建为 0 error、0 warning。

该修改只允许重新取得完整 S1 辨识数据，不构成 MPC 硬件接管授权；`hardware_takeover_allowed=false`。
