# `0x59490501` C 组采集与冻结模型复核

日期：2026-09-11。目标机目录：
`/home/nuc11--02/yaw-cd-trace-59490501/captures/cd_c_20260911_154414/dual_c`。
本地原始副本：`NoMachineTemp/cd-c-20260911-154414/dual_c`。

## 采集结论

C 组通过采集放行，可以在保持右拨杆 UP 后 RELEASE，并进入一次性 D 采集。它不构成模型独立验证，
也不允许 MPC 接管。

- 固件 `0x59490501`、trace v5、profile 4、phase-set C；
- phase 5、reason 0、`error=null`、CRC 0、`quality_issues=[]`；
- 9000条，比标称9001少1条；`trace_us`覆盖35.999875秒，终止时刻36,000 ms；
- 采样间隔1.850～5.094 ms，中位3.999 ms，完整时长判据通过，后续按真实时间重采样；
- 两轴 CAN failed/errors/aborted均为0，没有计数饱和或中间覆盖缺口；
- 最大反馈年龄汇总为大轴1.216 ms、小轴2.514 ms。

原始流 SHA256：
`EB31C10D351904896D90E30A9FAE64F86EEF11EED10D07FF12DF5A0482F24B0A`。
现场 `report.json` SHA256：
`3126714D4A54A77E6849D88A7B79FDD526BA8555C797D8A2FD1C15E71FABC0E6`。
独立回放与现场报告一致。

## 参考、运动和输入

- 冻结计划 SHA256正确；逐点参考最大误差0.00836°，低于0.011°门槛；
- 激活段参考秩为2，两轴相关系数−0.02356；
- 实测相对起点峰值：大关节1.846°、小关节2.812°、IMU航向1.434°；
- 尝试软件命令范围：大轴−0.474～+1.235，小轴−1.171～+0.567，明显低于30/6限制；
- 前3秒反馈电流中位数：大轴−62、小轴−147 raw；全程范围分别为−419～882、−976～283 raw；
- 因果重采样后的反馈年龄最大为大轴4.832 ms、小轴6.367 ms。

## C-only 冻结结果

结构严格使用 A/B 预先选定的 `output_lags=12`、`reference_lags=4`、`delay=0`、`ridge=1.0`，
没有在 C 上重新扫描结构。拟合后自治谱半径为0.986982，36秒预测有限。

C 激励段属于拟合内检查：相对保持基线的大/小/航向改善为36.9%/39.4%/65.3%。回稳段模型弱于
局部保持基线，尤其小关节存在约1.11° RMSE的慢偏差；因此不能根据 C 宣称模型通过，必须保持
D 完全独立。

电流诊断在 C 内冻结为：大轴增益0.7480、延迟0采样、时间常数16 ms；小轴增益0.7451、延迟2采样、
时间常数0 ms。偏置仍混合传感器零偏、负载和闭环保持作用，不能解释为力矩。

冻结 C 分析报告：`NoMachineTemp/cd-c-analysis-20260911-v2/report.json`，SHA256：
`F4CADD0D94E98FBA9ED0D61E5B545FDA91D4A52AA2E16B0E9E607B7D0A3D40C6`。
分析程序 SHA256：
`8FD70CB0C20DBC3E2B3B5191C66CA463DD44F785C1351DD69E1FEF0409764320`。

## D 前冻结的检验

在读取 D 之前已经生成 `validate_cd_reference_offline.py`，SHA256：
`C087B8914C6D7A7E9AFB53534B27A2F0E5ED6A699D937ADCF836567546F82799`。它强制读取上述 C 报告哈希，
禁止重拟合 D 运动模型，并固定以下门槛：

1. D 激励段大、小关节分别相对保持基线至少改善30%；
2. C/D独立电流增益相对差不超过10%；
3. 电流等效延迟差不超过1个4 ms采样；
4. 完整预测必须有限；D参考、时序、CAN和数据质量门槛必须先通过。

无论 D 是否通过，结果仍是当前 PID 下的闭环参考模型，保持
`hardware_takeover_allowed=false`。
