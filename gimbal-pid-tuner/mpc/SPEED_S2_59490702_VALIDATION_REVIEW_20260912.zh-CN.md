# `0x59490702` S2 冻结模型验证复核

日期：2026-09-12；S2 来源：目标机
`/home/nuc11--02/yaw-speed-trace-59490702/captures/speed_s2_20260912_002350/s2`。

## 正式结论

S2 **未通过采集前冻结的数据门，因此不能正式接受模型**。原因只有一项：取得 8994/9001 条，短缺
7 条，超过冻结上限 5 条。主机据此给出 `sample_count_not_9001` 和
`full_duration_timing_accepted=false`。不能在查看 S2 后把上限由 5 改为 7，也不重复 S2。

这不是运动或模型数值失败。S2 正常运行满 36 秒，`phase=5`、`reason=0`、CRC 0、CAN
failed/errors/aborted 全为 0、无反馈故障、无固件流质量锁存、无停止保护。原始流独立 replay 完整复现
8994 条和同一个数据门告警。

## 冻结模型的事后诊断

在保持模型、归一化、20 ms 重采样、极性、窗口和阈值完全不变的情况下，对可用的完整时间跨度做单独
诊断。该结果不能推翻正式拒绝：

| 预测长度 | 大轴 RMSE | 小轴 RMSE | 相对保持改善（大/小） |
|---:|---:|---:|---:|
| 100 ms | 3.323°/s | 2.371°/s | 42.6% / 80.9% |
| 200 ms（冻结主指标） | 3.329°/s | 2.496°/s | 48.1% / 80.9% |
| 500 ms | 3.348°/s | 2.572°/s | 50.9% / 79.9% |

如果数据门曾通过，200 ms 的四个冻结数值条件都会通过：大/小改善门为 20%/50%，RMSE 门均为
6°/s。由此可知 S2 没有显示模型跨频率失效，但它仍不能提供预声明规则下的正式独立通过。

## 缺样性质

记录时间跨度为 `35.999885 s`；原始间隔中位数 4001 µs、99% 分位 4835 µs、最大 6023 µs，
没有超过 8 ms 的间隔。20 ms 网格每个点两侧最近原始样本的最坏距离为 4.815/4.096 ms。因此短缺
主要是 250 Hz 记录调度抖动，不像串口截断、连续丢帧或运动中止。这支持保留诊断数值，但不能事后改门。

## 证据与状态

- 冻结模型 SHA256：`1E81D3F94F3C81E3CE5A9A04481AABFCFE0270A8753BC4D3CE2C7A7725A2D53D`；
- S2 raw SHA256：`377ACE39886D9EE14CD9F4189E73012EF4BEBFFF230A2A64E6523AC7C83F90D6`；
- 诊断 JSON：`NoMachineTemp/speed_s2_59490702_20260912_002350/diagnostic_after_formal_rejection.json`；
- S2 已在 `flags=253`（UP）时 RELEASE，最终 probe 为 `phase=0`、`count=0`、`reason=0`、
  `flags=252`。

当前状态为 `speed_reference_model_numerically_supported_but_formal_S2_data_gate_failed`，
`hardware_takeover_allowed=false`。若要取得正式独立证据，应先设计并冻结一个不同频率集的 S3，且把数据
门改为与 20 ms 重采样需求直接对应的时域覆盖门；不能继续使用已经查看过的 S2 调门或重采。
