# 大 yaw 慢速采集复核

## 来源与结论

- 固件0x59490203，trial id=2，大yaw目标±15°，20秒，5001条。
- 小车PC文件：`/home/nuc11--02/yaw-slow-59490203-feedback/captures/big_slow_amp15_20260909_212307`。
- 本机副本：`NoMachineTemp/big_slow_amp15_20260909_212307`，包含原始流、CSV及报告。
- phase=5、reason=0，CRC错误0，feedback_failure=null。严格质量标记仍为irregular_sample_interval。
- 本轮保留用于离线探索，不单凭轻微时间抖动要求重采。尚未拟合或验证模型，禁止据此接管硬件。

## 时间与反馈

5000个相邻时间间隔中，4998个4ms、1个5ms（索引802，3.209秒）、末尾1个3ms。
总时长20秒。3.209秒后样本相对理想网格偏移1ms，最终终止样本回到20秒边界。
不修改原始数据或移除质量告警；后续模型处理须明确使用实际时间戳，或记录重采样过程。
IMU数据年龄最大3ms，大yaw最大1ms，小yaw最大3ms。

## 实际响应

编码器按8192计数/圈换算。大yaw相对初始角做±180°差分；小yaw相对标定零点，
航向相对冻结的初始IMU航向。这里只做数据统计，不由此推断力矩或系统惯量。

| 量 | 最小 | 最大 |
| --- | ---: | ---: |
| 大yaw相对起点角度 | -14.063° | +15.249° |
| 小yaw关节角 | -12.217° | +12.700° |
| IMU航向相对起点 | -0.952° | +0.732° |
| 大yaw记录转速 | -4 rpm | +4 rpm |
| 小yaw记录转速 | -5 rpm | +3 rpm |
| 大yaw原始软件指令 | -1494 | +1616 |
| 小yaw原始软件指令 | -1230 | +1559 |

末段19~20秒，大yaw平均相对角为-1.218°，小yaw关节平均角为+0.940°，航向平均偏差+0.183°。
因此不应把“正常完成”解释为完全无静差或理想跟踪。该轮未报告限位/输出饱和。
双轴反馈明显变化，激励比此前短小幅度采集充分；能否得到可用模型仍取决于小yaw激励及独立验证。

## 下一步

保持相同PID、负载、Pitch姿态、固定车体。上置释放后采集小yaw航向±10°、20秒，
大yaw保持起始关节角。严格保留限位余量检查；BENCH过期先重做并释放。
暂不增加输出上限、不修改PID、不部署MPC。下一轮先检查质量及响应，再规划重复/反向验证集。
